import Vue from 'vue';
import Vuex from 'vuex';
import * as blockstackAuth from '@/store/modules/blockstackAuth';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    blockstackAuth,
  },
});
